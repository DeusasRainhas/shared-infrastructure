---
# Host-specific overrides
hostname: "stg.deusasrainhas.local"
1nfs_storage_path: /storage/hosts/deusasrainhas/stg
nfs_mount_path: /home/{{ansible_user}}/storage
storage_mounts:
  - src: "{{ nfs_storage_server }}:{{ nfs_storage_path }}"
    dest: "{{ nfs_mount_path }}"
    fstype: "nfs"
    opts: "rw,defaults"
    state: "mounted"
